import { PlaceholderScreen } from "@/components/placeholder-screen";

export default function HomeScreen() {
  return (
    <PlaceholderScreen
      title="Your backlog is empty"
      systemImage="gamecontroller"
      description="Add the games you own and Barklog will keep track of what you're playing, what's up next, and what you've finally finished."
    />
  );
}
